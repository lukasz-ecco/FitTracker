{{ form_start(form, {'attr': {'id': 'edit-measurement-form'}}) }}
    <div class="mb-4 bg-gray-50 p-3 rounded-md">
        <span class="block text-sm font-medium text-gray-500">Część ciała:</span>
        <span class="mt-1 block text-lg font-semibold text-gray-900">
            {{ meseurment.bodyPart ? meseurment.bodyPart.name : 'Brak danych' }}
        </span>
    </div>

    <div class="mb-4">
        {{ form_label(form.Size, 'Rozmiar', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
        {{ form_widget(form.Size, {'attr': {'class': 'mt-1 block w-full rounded-md border-gray-300 shadow-sm'}}) }}
    </div>

    <div class="mb-4">
        {{ form_label(form.date, 'Data', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
        {{ form_widget(form.date, {'attr': {'class': 'mt-1 block w-full rounded-md border-gray-300 shadow-sm'}}) }}
    </div>

    <div class="mt-6 flex justify-end">
        <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
            Zapisz zmiany
        </button>
    </div>

{{ form_end(form) }}