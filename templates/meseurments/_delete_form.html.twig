<form method="post" action="{{ path('app_meseurments_delete', {'id': meseurment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meseurment.id) }}">
    <button class="btn">Delete</button>
</form>
