{% extends 'base.html.twig' %}

{% block title %}Mięśnie - szczegóły{% endblock %}

{% block body %}
    <h1>Wyświetl mięśnie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ muscle.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ muscle.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_muscles_index') }}">Wróć do listy</a>

    <a href="{{ path('app_muscles_edit', {'id': muscle.id}) }}">Edytuj</a>

    {{ include('muscles/_delete_form.html.twig') }}
{% endblock %}
