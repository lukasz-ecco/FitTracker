<form method="post" action="{{ path('app_muscles_delete', {'id': muscle.id}) }}" onsubmit="return confirm('Czy na pewno chcesz usunąć ten element?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ muscle.id) }}">
    <button class="btn">Usuń</button>
</form>
